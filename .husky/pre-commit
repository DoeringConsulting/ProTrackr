#!/usr/bin/env sh

# Tests run here so we fail fast even before the user finishes typing a
# commit message. The version bump and build are deferred to
# prepare-commit-msg (which runs after the message is known).
echo "[pre-commit] Unit-Tests werden ausgeführt..."
npx vitest run server/taxEnginePl.test.ts server/uiValidationReportsDashboard.test.ts
